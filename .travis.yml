language: python
python:
  - '3.6'
node_js:
  - stable

install:
  - 'pip install pipenv'
  - bash .travis/_install.sh

script:
  - bash .travis/_build.sh
  - bash .travis/_deploy.sh

branches:
  only:
    - master

cache:
  packages: yes
  directories:
    - $HOME/.cache/pip
    - $HOME/virtualenvs
    - $HOME/.local/share/virtualenvs
    - $HOME/.npm
    - node_modules
